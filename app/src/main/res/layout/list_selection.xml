<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginLeft="5dp" android:layout_marginRight="5dp">

    <ImageView
        android:id="@+id/selectionIcon"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/selection_title"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="-2dp"

        android:layout_width="@dimen/option_button_size"
        android:layout_height="@dimen/option_button_size" />

    <TextView
        android:id="@+id/selection_title"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@id/selectionIcon"
        android:text="structure"
        android:textStyle="bold"
        android:textAlignment="center"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/blue"
 />

    <TextView
        android:id="@+id/cost"
        android:background="@drawable/option_bubble_tight"
        app:layout_constraintTop_toTopOf="@id/selectionIcon"
        app:layout_constraintRight_toRightOf="@id/selectionIcon"
        app:layout_constraintLeft_toLeftOf="@id/selectionIcon"
        android:text="$0"
        android:textStyle="bold"
        android:textAlignment="center"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/gold"/>

</androidx.constraintlayout.widget.ConstraintLayout>